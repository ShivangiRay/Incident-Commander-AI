# Incident Data Model

The model centers on incident, signal, service, deployment, hypothesis, evidence link, action recommendation, policy decision, postmortem, and memory case entities. See `libs/common/models.py` for executable schemas.

